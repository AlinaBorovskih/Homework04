int[] mass = new int[8];
{
   for (int i = 0; i < mass.Length; i++)
    {
       mass[i] = int.Parse(Console.ReadLine());
    }

for (int i = 0; i < mass.Length; i++ )
{
Console.Write($"{mass[i]} ");
}
}